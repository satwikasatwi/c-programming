#include <stdio.h>



int main() {
int num,sum=0,r,n;
printf("enter a positive number:");
scanf("%d",&num);
n=num;
while(num>0)
{
    r=num%10;
    sum=sum+r;
    num=num/10;
}

if(n%sum==0)
{
printf("%d  is a harshad number",n);
}
else
{
printf("%d is not a harshad number",n);
}

    return 0;